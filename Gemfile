source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in dry-system-hanami.gemspec
gemspec

group :test do
  gem 'dry-system', require: false
end
